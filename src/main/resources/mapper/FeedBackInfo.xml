<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blackfiresoft.aiproject.feedback.mapper.FeedInfo">
    <insert id="insertInfo" parameterType="com.blackfiresoft.aiproject.feedback.pojo.FeedInfoModel">
        insert into feedinfo(openid,message,telNumber) values(#{openid},#{message},#{telNumber});
    </insert>

</mapper>